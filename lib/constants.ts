/**
 * Base path for the app, matching the value in next.config.ts.
 * Used to prefix static asset paths (images, etc.) that need
 * the subpath when deployed to GitHub Pages.
 */
export const BASE_PATH = "/Bitacora-Fit-Web";
